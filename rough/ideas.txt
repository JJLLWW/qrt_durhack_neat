- does it need the detailed information from the long info messages.
- mainstream server side python framework? Flask / Django / Fast API <- Django REST Framework
<- does Django work when you want something thats mostly API focused?
Spotify + Instagram use Django so it does scale + have some industry relevance
Django appears to only expose HTTP based endpoints by default, no GRPC etc.

how would django support data that isn't already in a DB? Models map to db tables ONLY

WS messing around:
python -m websockets ws://localhost:8001/

dataclasses/datetimes arent serialisable with json.dumps() <- wtf python
will jsonpickle 3rd party lib work with datetime? <- supposedly handles entire dataframes.